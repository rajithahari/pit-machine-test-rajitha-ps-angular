import { Component } from '@angular/core';

@Component({
  selector: 'register',
  template: `<div class="center"><button routerLink="register" routerLinkActive="active" type="button" class="btn btn-primary">Register</button></div>`
})
export class RegisterComponent  {
}

